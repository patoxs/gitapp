{% extends 'base.html.twig' %}

{% block title %}TypeBranch{% endblock %}

{% block body %}
    <h1>TypeBranch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_branch.id }}</td>
            </tr>
            <tr>
                <th>Type_branch</th>
                <td>{{ type_branch.typeBranch }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_branch_index') }}">back to list</a>

    <a href="{{ path('type_branch_edit', {'id': type_branch.id}) }}">edit</a>

    {{ include('type_branch/_delete_form.html.twig') }}
{% endblock %}