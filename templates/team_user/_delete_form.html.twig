<form method="post" action="{{ path('team_user_delete', {'id': team_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_user.id) }}">
    <button class="btn">Delete</button>
</form>